<p id="notice"><%= notice %></p>

<p>
  <strong>Name:</strong>
  <%= @product.name %>
  
</p>
<p>
  <strong>Image:</strong>
  <% if @product.image.attached?  %>
    <image src="<%=(url_for(@product.image))%>">
  <% end %>
  
</p>


<%= link_to 'Convert', convert_product_path(@product) %> |
<%= link_to 'Edit', edit_product_path(@product) %> |
<%= link_to 'Back', products_path %>
