<p id="notice"><%= notice %></p>

<p>
  <strong>Name:</strong>
  <%= @product.name %>
  
</p>

<ul>
  <% @product.images.each do |image| %>
    <li>
      <img src="<%=url_for(image.image)%>">
    </li>
    <li>
    <% key=image.image.blob.key%>
    ./storage/<%=key[0,2] %>/<%=key[2,2]%>/<%= key%>
    </li>
  <% end %>
</ul>

<%= link_to 'Edit', edit_product_path(@product) %> |
<%= link_to 'Back', products_path %>
